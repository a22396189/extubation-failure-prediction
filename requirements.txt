# 執行環境：Python 3.10.19（Miniconda，conda 環境名稱 extubation_env）
# 安裝方式：
#   conda create -n extubation_env python=3.10
#   conda activate extubation_env
#   pip install -r requirements.txt
#
# 註：torch / torchvision / torchaudio 為 CUDA 12.4 版本（對應 NVIDIA GPU 驅動與 CUDA Toolkit）。
#     若在 CPU-only 或不同 CUDA 版本的機器上安裝，請至 https://pytorch.org/get-started/locally/
#     依實際環境選擇對應安裝指令，其餘套件版本不受影響。

--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.6.0+cu124
torchvision==0.21.0+cu124
torchaudio==2.6.0+cu124

numpy==2.2.6
pandas==2.3.3
scipy==1.15.2
scikit-learn==1.7.2
xgboost==1.7.6

duckdb==1.4.1

matplotlib==3.10.7
seaborn==0.13.2
Pillow==11.3.0

shap==0.49.1
captum==0.9.0
optuna==4.8.0
umap-learn==0.5.9.post2
lifelines==0.30.0
statsmodels==0.14.5

joblib==1.5.2
tqdm==4.67.1
